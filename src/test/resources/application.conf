mongo {
  # All parameters except dbName are ovveriden by Testcontainers
  dbName = "receipt_db_test"
  uri = "not_used"
  host = "not_used"
  port = 27017
  user = "not_used"
  pass = "not_used"
}

verificationService {
  clients {
    apiPeru {
      url = "http://dummy-url"
      token = "dummy-test-token"
      timeoutSeconds = 1
    }
    factiliza {
      url = "http://localhost"
      token = "dummy-test-token"
      timeoutSeconds = 1
    }
    jsonPe {
      url = "http://localhost"
      token = "dummy-test-token"
      timeoutSeconds = 1
    }
  }
}

bonusService {
  bonusClient {
    url = "http://localhost"
    appCode = "dummy-test-app-code"
    lang = "en"
    timeoutSeconds = 1
  }
}